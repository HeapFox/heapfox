export async function login() {
  // MVP: prompt for a project token and store it in local file
  console.log('Login (MVP): please paste your project token')
  console.log('Tip: in future this will open a browser OAuth flow.')
}
